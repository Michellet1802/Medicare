<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicare">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/healthcare"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.medicare.DoctorUI.MiPacienteExpediente"
            android:label="@string/title_activity_my_patient_medical_folder"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name="com.example.medicare.DoctorEspecialidad" />
        <activity
            android:name="com.example.medicare.DoctorUI.DoctorCita"
            android:label="@string/title_activity_doctor_appointments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.example.medicare.DoctorUI.DoctorEditarPerfil" />
        <activity android:name="com.example.medicare.DoctorUI.PantallaDoctorPerfilInf" />
        <activity android:name="com.example.medicare.DoctorUI.MiPacienteInf" />
        <activity android:name="com.example.medicare.DoctorUI.MiPacienteActivity" />
        <activity android:name="com.example.medicare.DoctorUI.MostrarInfPaciente" />
        <activity android:name="com.example.medicare.MiDoctorInfoActivity" />
        <activity android:name="com.example.medicare.MisDoctoresActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.medicare.DoctorLocalizacion"
            android:label="@string/title_activity_doctor_localisation" />
        <activity android:name="com.example.medicare.InfoConsulta" />
        <activity
            android:name="com.example.medicare.ExpedienteMedico"
            android:label="@string/title_activity_medical_folder"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.example.medicare.DoctorUI.BuscarPaciente" />
        <activity
            android:name="com.example.medicare.AsignarCitaActivity"
            android:label="@string/title_activity_appointments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.example.medicare.ProgramarCitaActivity" />
        <activity android:name="com.example.medicare.DoctorUI.DoctorMenuActivity" />
        <activity android:name="com.example.medicare.EditarPerfilActivity" />
        <activity android:name="com.example.medicare.PerfilPacienteInfo" />
        <activity android:name="com.example.medicare.DisplayDoctorInfo" />
        <activity android:name="com.example.medicare.MostrarActividad" />
        <activity
            android:name="com.example.medicare.BuscarDoctorEspecialidadActivity"
            android:label="@string/title_activity_search_doctor_speciality"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.example.medicare.BuscarDoctorActivity" />
        <activity android:name="com.example.medicare.MenuActivity" />
        <activity android:name="com.example.medicare.CrearCuenta" />
        <activity android:name="com.example.medicare.MainActivity" />
        <activity android:name="com.example.medicare.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>